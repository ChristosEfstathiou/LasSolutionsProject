# Receiving / Putaway Configuration

PREFER_SAME_PRODUCT_LOCATION=Y
ALLOW_SPLIT_STORAGE=N //for future implementation, not currently supported
ENABLE_RECEIPT_LOGGING=Y
DEFAULT_RECEIPT_STATUS=RECEIVED 

DB_USER=warehouse
DB_PASS=warehouse123
DB_HOST=localhost
DB_PORT=1521
DB_SERVICE=FREEPDB1